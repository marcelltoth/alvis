{
  "name": "alvis",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:596050/alvis.git",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"cd server && cd code-execution && yarn run start:dev\" \"cd client && yarn run start\"",
    "clean:modules": "cd client && rm -rf node_modules && cd server && cd code-execution && rm -rf node_modules",
    "install:all": "cd server/code-execution && yarn && cd ../../ && cd client && yarn",
    "reset": "yarn clean:modules && yarn && yarn start"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
